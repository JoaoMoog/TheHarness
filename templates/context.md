# Repository context

Generated by the codebase-inventory skill. Committed on purpose: it is read at
the start of every session, so the expensive discovery happens once per
repository rather than once per task.

Regenerate it when the structure changes, not on every commit.

generated: <YYYY-MM-DD>
commit: <short sha the inventory describes>

## What this repository is

Two or three sentences. What it does, who runs it, what breaks if it stops.

## Stack

| layer | technology | version |
|---|---|---|

## Entry points

Where execution actually begins: the server bootstrap, the CLI, the job
handlers, the infrastructure root module.

| entry point | file | notes |
|---|---|---|

## Commands

The commands an agent may run. If a command is not here, it has not been
sanctioned.

| purpose | command |
|---|---|
| install | |
| build | |
| test | |
| lint | |
| run locally | |

## Module map

The top-level modules and what each one owns. Ten to twenty rows; if it needs
more, the interesting unit is a sub-tree and this is the wrong altitude.

| module | owns | depends on |
|---|---|---|

## Conventions this repository already follows

Observed from the code, not aspirational. Error handling shape, logging,
naming, test layout, layering rules.

## Landmines

The things that look wrong and are load-bearing, and the things that look fine
and will break. This section is the one that saves the most time.
