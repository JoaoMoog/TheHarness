---
description: Update tasks inside the consolidated plan.
version: 3.0.0
agent: orchestrator
---

Use @planner to update the task list from the existing plan. Do not reapprove an unchanged approved scope.

Request: ${input:request}
